jQuery(document).ready(function ($) {

    $(document).snowFlurry({
        maxSize: 12,
        numberOfFlakes: 80,
        minSpeed: 15,
        maxSpeed: 20,
        color: 'Cornsilk',
        timeout: 0
    });

});